from enum import Enum

class GenderEnum(Enum):
    """
    Enumeration for representing gender options.

    Attributes:
        MALE (str): Represents the male gender, value is "M".
        FEMALE (str): Represents the female gender, value is "F".
    """
    MALE = "M"
    FEMALE = "F"